// import { requestAnimals, requestAnimalsWithError } from "./api";


import Requirements from "./Requirements";

// Примеры вызова функций, в консоли можно увидеть возвращаемые результаты
// requestAnimals({ animal: "", amount: "2", limit: 5, offset: 0 }).then(
//   console.log
// );
// requestAnimalsWithError({ animal: "", amount: "", limit: 4, offset: 0 }).catch(
//   console.error
// );

export default function App() {
  return <Requirements />;
}
